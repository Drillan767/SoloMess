<%= form_with(model: article, local: true) do |f| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, id: :article_title %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image, id: :article_image %>
  </div>

  <div class="field">
    <%= f.label :tags %>
    <%= f.text_field :tags, id: :article_tags %>
  </div>

  <div class="field">
    <%= f.label :public %>
    <%= f.check_box :public, id: :article_public %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content, id: :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>